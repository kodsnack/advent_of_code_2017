function steps(input){
  var pos = [[]];
  pos[0][0] = 1;
  pos[0][1] = 2;
  var COLS = 10;
  var a = 0;
  var b = 2;
  var y = 0;
  var x = 1;
  var matrix = [[1]]
var ym;
var xm;
var x,y = 0,0;

// Printing inwards to outwards. Works only for odd sized 2D array with one element in center
while (true) {
    for(; y < ym; y++, b++) // go right
        pos[y][x] = b;
}

}
steps(10);